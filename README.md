# ngrx-app

node version - 14.17.0
angular version - 12
set NODE_OPTIONS=--openssl-legacy-provider // for any lagecy version issue while running application.
npm install issue for version - npm install --save --legacy-peer-deps

# output screenshots added as part of source for verify the work done.
